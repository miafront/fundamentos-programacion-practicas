#include <iostream>
#include <string>
#include <cctype>
using namespace std;
string mayusculas(string texto){
for(int i=0;i<texto.length();i++)
texto[i]=toupper(texto[i]);
return texto;
}
int main(){
string texto;
getline(cin,texto);
cout<<mayusculas(texto);
return 0;
}
